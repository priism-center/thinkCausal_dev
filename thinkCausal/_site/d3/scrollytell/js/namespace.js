// helps manage namespace collisions across learning modules
// all js objects/functions should be stored within this object
// e.g. estimands.myFunction = function(x){...}
var estimands = estimands || {};
