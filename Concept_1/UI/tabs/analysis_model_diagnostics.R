analysis_model_diagostics <- tabPanel("Model", 
                                   sidebarLayout(
                                     sidebarPanel(
                                       h4('Model Diagnostics'), 
                                     ),
                                     mainPanel()
                                     ))
