welcome_dropdown <- tabPanel(title = 'Welcome', 
                               h4("thinkCausal"), 
                               mainPanel(
                               includeMarkdown('UI/markdowns/landing.md'))
)